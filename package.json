{
  "name": "aminrahman-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "^5.5.0",
    "vite": "^5.2.0"
  }
}
